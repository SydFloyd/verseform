# Verseform — Tight Roadmap

## Delivery rule

This roadmap is intentionally short. Each item is an end-to-end user outcome with its contract, implementation, failure behavior, and proof included. Do not create separate foundation milestones or one item per toolbar button. A newly discovered defect stays inside the active item unless it is independently shippable or changes product scope.

Status: `done`, `next`, `queued`, or `blocked`.

| ID | Status | Outcome | Exit proof |
|---|---|---|---|
| `VFM-000` | done | Align the product contract, architecture, decisions, agent loop, and work state. | The five authorities are linked, non-duplicative, and identify the next slice and external inputs. |
| `VFM-010` | done | Prove the walking scripture-document slice. Scaffold the Windows Tauri/React/Tiptap app and browser harness. Type one supported reference, decorate it after a delimiter, preview through a fake provider, click to insert an editable marked citation, undo once, save/reopen `.verseform`, and produce one print/PDF sample with attribution and optional page number. | A browser test proves the full interaction and stale-response rejection; Windows smoke proves atomic save/reopen and the selected WebView2 output route. Decisions `D-004`, `D-005`, and `D-008` are confirmed or superseded. |
| `VFM-020` | done | Make ordinary editing and local documents trustworthy. Add the required fonts and formatting, hyperlinks, lists, indentation, alignment and spacing, find/replace, spellcheck, clean paste, native Open/Save/Save As, recent files, dirty-close behavior, autosave, crash recovery, and document migrations. | Formatting survives fixture-backed save/reopen and print rendering; keyboard paths work; kill/restart restores accepted work; corrupt or newer files fail without overwriting originals. |
| `VFM-030` | done | Complete local reference intelligence. Support approved book names and abbreviations, conservative fuzzy book matching, single verses and ranges, translation-aware canon bounds, punctuation, valid/invalid styling, hover anchoring, citation exclusion, and false-positive control. | A versioned corpus covers every supported book/alias, boundary, range, invalid class, citation case, and representative prose false positive; detection remains responsive on a long document and makes no network call. |
| `VFM-040` | done | Connect authorized scripture. Implement the DBS adapter and translation catalog, local default translation, cancellation/timeout/schema limits, attribution metadata, explicit WEB fallback, preview, one-click insertion, and permitted caching policy. | Fake, bundled WEB, and recorded DBS adapters pass one provider contract; an opt-in live ARC smoke passes without a secret; browser cases prove selection, persistence, chapter caching, offline startup, failure fallback, cancellation, attribution, and stale-response rejection; the full repository check and Windows desktop build pass. |
| `VFM-050` | done | Finish print and PDF. Render immutable snapshots with faithful formatting, optional page numbers, default DBS footer, and all translation-specific notices; integrate Windows print and Save PDF flows. | Automated PDF checks find expected text, page numbers, footer, and notices; representative documents pass visual review; cancellation, unwritable paths, and provider-free offline output preserve the open document. |
| `VFM-060` | done | Harden and ship the Windows alpha. Close accessibility and keyboard gaps, bound large-document behavior, handle low disk and interrupted writes, audit dependencies/licenses/capabilities/CSP, build the installer, and document privacy, supported behavior, and known limits. | A clean Windows machine installs, launches offline, completes detect-to-insert/save/reopen/print/PDF, and uninstalls without deleting user documents; the release checklist and focused regression suite pass. |
| `VFM-070` | done | Refine the alpha editor chrome and defaults from owner testing. Use familiar File/Edit menus and formatting icons, expose paragraph settings in a dialog, synchronize font/size controls and the Windows title, recognize paragraph breaks as reference delimiters, and prefer NASB for a first online session while retaining an explicit saved preference and offline WEB. | Browser tests prove the menu and keyboard paths, hidden dialogs, live formatting state, newline detection, title changes, NASB online selection, saved preference behavior, and offline WEB fallback; the repository check and Windows desktop build pass. |
| `VFM-080` | done | Plan the Beta as one coherent control system. Reconcile the product, state model, effect ownership, editor boundary, command surface, trust boundaries, diagnostic contract, UX direction, verification economy, and delivery sequence around one application kernel. | Requirements, system design, decisions, roadmap, and `WORK.md` agree on the canonical state regions, intent→event→effect→result loop, invariant-to-proof map, behavior-preserving refactor boundary, compact Beta UX, DBS credits surface, next slice, and Beta acceptance. |
| `VFM-090` | done | Replace monolithic React orchestration with the application kernel while preserving Alpha behavior. Introduce workspace transitions/selectors, operation stamps, fake scheduler, controller, finite command registry, editor gateway, and redacted browser diagnostics; migrate document/persistence, scripture, and output/overlay flows vertically and remove each superseded control path. | Pure tables cover startup, edit/dirty, recovery/autosave, explicit save, late save, new/open/close confirmation, catalog/preference/fallback, preview/insertion freshness, output cancellation/failure, notices, command enablement, and overlay exclusivity. React imports no concrete adapter and performs no runtime effect; Tiptap is reachable only through the editor boundary outside editor components; existing browser behavior remains unchanged; `npm run check` and the Windows desktop build pass. |
| `VFM-100` | done | Hone the daily-use Beta UI without adding feature breadth. Remove the duplicate visible Verseform page heading; tighten the same flat menu/toolbar design with stable groups and quiet-at-rest command chrome; present the active translation as a compact abbreviation that expands into searchable full titles; preserve accessible targets/focus; add Help → Credits & Licenses; and safely expose DBS, effective-translation, WEB, version, and software notices. | Browser visual/interaction cases at representative widths and Windows scale factors show more writing space, no clipped or shifting controls, boundary feedback on hover/focus/active state, abbreviation/full-title translation selection, pointer/keyboard command parity, correct focus return, no-network local credits, escaped provider metadata, and one allowlisted external-link path. Axe A/AA, the complete walking flow, `npm run check`, and the desktop build pass; the owner accepted the arrangement and final styling. |
| `VFM-110` | done | Stabilize and release the Windows Beta. Close daily-use feedback, including browser print preview and the dedicated immutable PDF review/export dialog, then exercise repeated document/scripture/output workflows, Alpha-to-Beta upgrade preservation, offline/online transitions, recovery and cache aging, failure clarity, resource bounds, installer lifecycle, privacy/license evidence, and final supported/known-limit documentation. | Focused regressions cover owner-reported editing, provider-text, scrolling, and output defects; output proof covers keyboard-contained PDF review, page-number changes without recapture, both cancellation layers, WebView2 browser-preview routing, and attributed multi-page export. A clean Windows runner and an upgraded Alpha installation preserve user documents, profile, recovery, and cache; the canonical suite and focused endurance/failure cases pass; the Beta installer completes install/offline launch/uninstall without deleting user data; the signed/unsigned state is explicit; the artifact, checksum, exact run, supported behavior, privacy, credits, and known limits are recorded. |
| `VFM-120` | done | Publish and operate the field Beta without expanding application authority. Create a durable public GitHub pre-release from the exact verified `0.2.0` artifact, make unsigned/SmartScreen status and privacy/credits legible, and provide a structured public feedback path that solicits no private writing or application data. | Tag `v0.2.0` resolves to the verified candidate; release installer, checksum, and evidence match the clean-runner bytes; the release is marked pre-release and links supported behavior, privacy, DBS/WEB credits, known limits, and feedback; the issue form renders from the default branch with its privacy acknowledgement; no telemetry, updater, account, or new runtime network authority is added. |
| `VFM-130` | done | Preserve current writing when restoring recovery. Route Restore through the document navigation confirmation, identify the recovery being opened, and retire or restamp the previous document's persistence and lookup operations. | Pure transitions and a browser journey begin with an available recovery plus a different dirty draft. Cancel retains the draft; Save preserves it before restoring; a canceled or failed Save never restores; Discard alone authorizes abandoning it. Delayed save/recovery/lookup results cannot adopt the wrong document identity, erase newer recovery, or strand persistence. The canonical check passes. |
| `VFM-140` | next | Make scripture insertion discoverable and reachable with a keyboard while preserving one-level Tab/Shift+Tab indentation. Provide a documented direct route between writing, the command deck, and detected references; put a short first-use walkthrough in the existing user-facing release documentation. | Starting with keyboard input alone, a user reaches a valid and an invalid reference, previews, inserts, undoes, changes translation, reaches File/Edit/Help, and returns to writing. The browser proof never uses `.focus()` or DOM injection to reach the controls being tested. A Windows keyboard/screen-reader check confirms the path. The walkthrough explains the delimiter, click replacement, Undo, actual translation/WEB fallback, Save versus autosave/recovery, reopen, output, and optional feedback. |
| `VFM-150` | queued | Make the dedicated PDF review show the pagination and repeated attribution of the PDF actually exported. Keep one frozen document and the continuous editing surface. Use plain user-facing labels instead of implementation language such as “Immutable output snapshot.” | Compare the preview and exported PDF for a one-page document and a representative multi-page document with mixed formatting and multiple translation notices. Page boundaries, page numbers, visible text, and notices agree; long notices are not clipped. Option changes use the same captured content. Cancel/failure/focus behavior remains covered, the actual Windows WebView2 export is inspected, and the canonical check passes. |
| `VFM-160` | queued | Deliver a repeatable corrected Windows candidate to the first independent tester and establish actual usability evidence. Finish the short handoff, preserve a durable verified upgrade baseline, exercise the installer on the tester's Windows environment, and observe the ordinary writing-to-scripture-to-output loop. | `VFM-130`–`VFM-150` pass; the patch candidate passes the clean Windows release gate without depending on an expiring Actions-only Alpha download. The exact installer/checksum/evidence and unsigned status are recorded, and any publication uses a new immutable patch version under owner authority. An independent user completes the session below; blockers are fixed and rechecked, other friction is triaged, and `WORK.md` distinguishes automated, owner, and independent-user evidence. |

## First-user preparation review — 2026-09-05

Review scope is this **Verseform** repository at `ddb3e23`, not Verseform Word. `VFM-120` records completed distribution work; it does not establish that an independent person has used the app. The owner reports that the first test user has not started. Keep the existing editor/kernel architecture and prioritize the remaining acceptance gaps over additional features or another refactor.

| Finding | Evidence and implication | Owning preparation |
|---|---|---|
| Restore can replace a different dirty draft without confirmation. | In the production browser harness, leave an earlier recovery, reload, type a new unsaved draft, then choose Restore. The earlier text replaces the current text with zero confirmation dialogs. `controller.restoreRecovery` sends `recovery.restore` directly; that transition sends `content.set` without the normal dirty-navigation gate and retains prior persistence state. | `VFM-130`; highest priority before handing over writing. |
| Keyboard activation is tested more strongly than keyboard reachability. | `reference-intelligence.spec.ts` calls `reference.focus()` before Enter. In a separate keyboard-only check, typing a reference then pressing Tab, Shift+Tab, Escape, or F10 leaves focus in the editor; Tab/Shift+Tab correctly indent/outdent. A direct documented reference/menu navigation path is missing. This finding does not claim that every possible browser or assistive-technology workaround was tested. | `VFM-140`; preserve indentation and provide an intentional navigation path. |
| PDF review does not show exported pagination. | `PdfExportDialog.tsx` displays the snapshot as screen HTML. An 80-line sample produces a 4,107px-tall continuous sheet with one footer and one literal “Page 1”; `core/output.ts` adds real page counters only in print CSS. Existing multi-page PDF extraction passes, but does not compare the preview's page layout. | `VFM-150`; fulfill the existing accurate-preview requirement. |
| Automated release evidence leaves human and hardware questions open. | The full local check passed on 2026-09-05 (39 pure/application, 33 browser, 11 native unit, 7 Windows smoke tests; live service tests skipped). Browser `deviceScaleFactor` scenarios and a clean Windows Server runner do not establish an uncoached desktop user's experience, real Windows text scaling, screen-reader interaction, or printer behavior. | `VFM-160`; one observed first-user session and targeted native checks, not another broad feature milestone. |
| Future patch verification depends on a retained Alpha artifact. | `.github/workflows/windows-beta.yml` unconditionally downloads Alpha from Actions run `33759181934`; the recorded retention ends `2026-09-17`. Preserve its verified bytes/checksum in an owner-approved durable location and consume that baseline without silently rebuilding it. | `VFM-160`; prepare this before the first patch release needs it. |

Local, ignored review evidence is in `artifacts/first-user-review.mjs`, `artifacts/first-user-review-results.json`, and `artifacts/first-user-review-preview.png`. The reproduction descriptions above remain the durable record; the scratch artifacts are not required for the next agent to understand the findings. Product fixes had not been implemented by that review; `VFM-130` subsequently closed the recovery-restore finding in source.

### First independent-user session

Use invented writing and an approximately 20–30 minute observed session. Give the tester the ordinary release page and quick-start material; record any coaching instead of counting coached completion as independent discovery.

1. Download and install the exact candidate, identify its version, and start writing. Record Windows version, WebView2 version, actual display/text scale, and any unsigned-installer friction.
2. Format a short document; discover delimiter-triggered detection; preview and insert a verse/range; undo; inspect an invalid reference; select a translation and explain which translation was inserted.
3. Save to a chosen location, reopen, understand autosave versus recovery, and restore a prepared recovery without losing a second draft. Exercise one cancellation and one failed destination with disposable data.
4. Start offline with WEB, then reopen online with the saved preferred translation. Compare the dedicated PDF review with a multi-page export and use an available printer or record that physical printing remains unverified.
5. Locate version/help/credits and the feedback route. Record task completion, hesitation, coaching, recoverability, and the tester's willingness to use it again; collect no private document, cache, or recovery files. The owner may transcribe optional feedback for a tester without GitHub access.

Do not close this item using automated journeys in place of a person. The first-user gate is complete when there are no unresolved writing-loss, scripture-correctness, or core-task blockers; fix observed blockers with the cheapest regression and retest the affected task. Use subsequent users to decide broader usability work.

## Scope gates

- `VFM-010` is the only bootstrap gate. If the central interaction or output route is wrong, change it before broad formatting work.
- The DBS gate was resolved in `VFM-040` by the public ARC JSON contract and owner-confirmed cache permission (`D-013`–`D-015`).
- `VFM-090` is behavior-preserving. Do not mix visual redesign, document-format change, provider change, or new user capability into the kernel migration.
- `VFM-100` consumes the kernel's command/view-model contracts. Do not begin it by adding another UI-local control path.
- No second operating system, cloud feature, DOCX work, or editable page-layout feature enters this roadmap without an explicit product decision.
- A browser-version feasibility assessment is not approval to build or distribute it. Choose supported browsers, file/recovery guarantees, print/PDF behavior, and the DBS browser-access boundary before adding web implementation items. First-user Windows preparation remains the current sequence.
- Alpha feedback may reorder queued items, but it must not weaken document safety, citation correctness, attribution, or privacy.

## Completed Alpha baseline

A Windows user can install Verseform, write and format a document, detect valid/fuzzy/invalid references, preview and insert authorized scripture with one click, work offline with clearly identified WEB text, recover and reopen local work, and print or export an attributed PDF without creating an account.

## Definition of Beta

A daily Windows user receives the complete Alpha behavior through a compact, coherent interface whose visible state cannot disagree with application state; common commands behave identically from menus and shortcuts; documents survive Alpha upgrade, recovery, autosave, and late asynchronous results; DBS and license credits are locally inspectable; and the tested installer, privacy, attribution, accessibility, performance, and known-limit claims are accurate and reproducible.
