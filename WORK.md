# Verseform work state

Updated: 2026-09-03

## Now

- Stage: the verified Windows Alpha behavior now runs through the completed Beta control plane; `VFM-090` is complete.
- Active item: none; `VFM-090` is complete.
- Next item: `VFM-100` — use the kernel's command and view-model contracts to create the compact professional interface and local Help → Credits & Licenses experience.
- Baseline: one framework-free workspace kernel owns document, persistence, scripture, output, overlay, formatting, and notice state; its pure transition emits closed effects with operation stamps, and one injected controller runs them. Dirty/title/enablement are derived, Tiptap is isolated behind one editor gateway, React has no adapter access, menus and global shortcuts share one finite command catalog, and the browser harness exposes only a frozen redacted diagnostic. All prior Alpha document, provider, native, output, accessibility, and visible interaction behavior remains verified.
- Beta interaction target: remove the duplicate visible Verseform heading, tighten the flat command and formatting rows without reducing accessible targets, keep the writing surface primary at representative Windows widths/scales, and add local, safe DBS/WEB/software credits through the existing overlay/command/effect tower.

## Last verified

- `npm run check` passed on 2026-09-03: thirty-seven pure/provider/kernel/controller/architecture tests (plus one intentionally skipped live smoke), twenty-four production-browser tests, seven native unit tests (plus one intentionally ignored live smoke), and six Windows smoke tests, with clean TypeScript, Rust formatting, Clippy, CSP/capability checks, and 660 locked dependency-license records.
- Fourteen fast kernel/controller/architecture proofs cover startup, derived dirty/title/enablement, fake-scheduler cancellation, recovery/autosave, exact and late save, navigation confirmation, catalog/preference/fallback, preview/insertion freshness, immutable output cancellation/failure, notice ordering, overlay exclusivity, command uniqueness, and enforced React/editor/runtime boundaries. Browser proof adds the frozen redacted diagnostic while retaining all twenty-three Alpha journeys, including zero-violation axe, recovery, offline/provider fallback, output, and stale-response behavior.
- `npm audit --audit-level=high` reports zero vulnerabilities. RustSec scanned all 474 locked Rust packages and reports no vulnerabilities; allowed maintenance warnings remain in Tauri's transitive graph, and the reported unsound GTK crate is absent from the Windows target graph.
- `npm run build:desktop` produced the behavior-preserving kernel-refactor build as unsigned 4,214,972-byte `src-tauri/target/release/bundle/nsis/Verseform_0.1.0_x64-setup.exe` with SHA-256 `FE72F4007C281091F034D548A3F0F8FE8FA0C6A6E28C77011639724A3A575FBC`. The prior local lifecycle proof remains valid for the unchanged per-user packaging and uninstall boundaries.
- GitHub Actions Windows alpha run `33759181934` passed on a clean Windows Server 2025 runner at alpha-closeout commit `0c41ff0`: npm and Rust advisory audits, the canonical regression suite, a fresh NSIS build, the installer lifecycle, and artifact upload all succeeded. Artifact `verseform-0.1.0-windows-alpha` is retained through 2026-09-17; the same run independently proved the refinement cases and the complete provider-free offline detect-to-insert/save/reopen/print/PDF flow.

## Handoff rule

Replace the **Now** and **Last verified** sections as work advances. Keep blockers only while they remain actionable. Detailed history belongs in version control, not this file.
