# Verseform work state

Updated: 2026-09-07

## Now

- Stage: `VFM-180` browser implementation is complete in the same repository. The production web composition supports desktop Chrome/Edge, local drafts/recovery, import/download, real DBS and bundled WEB, browser Print/Save PDF, offline reopening, and the verified Windows download link. Accounts, cloud documents, mobile support, and other desktop operating systems remain outside scope.
- Active item: no implementation item remains open for this slice. Vercel linking/publication requires the owner's authenticated account/project: both the available browser session and normal CLI report logged out. No Vercel deployment was created. The complete Vercel build/output/security configuration and README setup are ready.
- Next delivery action: after Vercel sign-in, import `SydFloyd/verseform` at repository root with Node.js 22. `vercel.json` selects `npm run build:web` and `dist-web`; no environment variables are needed. Verify live NASB insertion, import/download, and offline reopening on the final HTTPS origin before declaring deployment verified. Never deploy the default deterministic browser harness in `dist/`. Keep a stable production domain so browser drafts remain accessible.
- Windows release: public immutable `v0.2.3` remains current. Web pushes now run ordinary application checks; Windows publication requires explicit workflow dispatch and a new version for new bytes. Do not dispatch the configured 0.2.3 workflow merely to publish the web app.
- Remaining product gate: `VFM-160` still needs the full independent 20–30 minute Windows session in the roadmap. Corrected-installer delivery and targeted shifted-reference/dirty-close retests are complete; browser automation does not replace independent usability, screen-reader, physical-printer, or actual Windows scaling evidence.
- Data boundaries: documents and recovery stay on the device; detection makes no request; only preview/insertion may retrieve passage text. Catalog/cache requests use anonymous bounded ARC fetches. The web bundle excludes harness controls and diagnostic/Tauri globals; the Windows privileged CSP still has no remote origin. Application updates never force a tab reload or delete drafts.
- Browser limitations: storage is origin/profile-specific and may be cleared or evicted; download important writing. Save a copy names an independent draft. Import creates a separate validated copy. Browser Print/PDF uses Letter at 100% with browser headers/footers off and cannot prove the user retained a file. Initial offline installation downloads about 1.55 MB of compressed JavaScript including WEB; wait for Ready offline.

## Last verified

- `npm run check` passed on 2026-09-07: 62 pure/provider/kernel/controller/architecture/Windows-readiness tests plus one skipped live smoke; 44 existing browser-harness journeys; 13 production web journeys; 11 native unit tests plus one ignored live smoke; 7 Windows smoke tests; TypeScript, Rust formatting/Clippy, release boundaries, and 678 dependency-license records. An initial gate caught an optional-argument compatibility detail in recovery cleanup; preserving the original call shape passed its focused proof and the full rerun.
- After correcting the development HTML entry path, `npm run test:web` passed again with all 13 journeys and TypeScript. Tests serve the actual Vercel output with its security headers and cover IndexedDB transactions, quota/denied storage, recovery after reopening, two-tab conflicts/copies, Blob downloads, citation-preserving import and delayed/corrupt imports, offline loading/failed installation/waiting updates, anonymous recorded HTTP through the real transport, WEB fallback, attributed PDF extraction, keyboard use, and Axe A/AA. Pure transport proof additionally covers streaming limits, cancellation, and stale/cache failures.
- Actual in-app browser smoke loaded the live DBS catalog and inserted John 3:16 from NASB in the production localhost preview, displaying the NASB citation and Ready offline. This is real browser/CORS evidence on localhost, not proof of the unpublished HTTPS deployment. The corrected dev server also renders the editor. The 900×680 captures `artifacts/vfm-180-web-editor.png` and `artifacts/vfm-180-web-help.png` were visually inspected; writing stays primary and help remains bounded/scrollable.
- Windows 0.2.3 was previously published by clean run `34079972025` at `87e84fcea7b4531d6166335747fdbdbe950aa851`. Exact public installer: 4,310,679 bytes, SHA-256 `95b1f9da1a36a6d3094dff44a7216d5a6830ce69e31510da04323c9b7e19e383`, explicit NotSigned. The attached checksum/evidence and public download agree. The release included complete-reference handling and the one-chapter-or-50-verses limit, durable Alpha upgrade preservation, and a separate clean offline lifecycle. See `BETA-RELEASE.md` for provenance.
- The Alpha baseline remains pinned in `.github/release-baselines/v0.1.0.json`: source `0c41ff0ad845533679611ccb4c5bf969ceaf5e0a`, 4,197,262-byte unsigned installer, SHA-256 `0caebf685ed7debfbb164b32a807871fb5d94026c471e1c03f23d2b025544001`. This web slice did not reinstall Verseform, alter the owner's Windows profile, rebuild a published installer, or change external DBS state.

## Handoff rule

Replace the **Now** and **Last verified** sections as work advances. Keep blockers only while they remain actionable. Detailed history belongs in version control, not this file.
