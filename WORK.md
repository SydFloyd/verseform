# Verseform work state

Updated: 2026-09-06

## Now

- Stage: `VFM-160` is in progress. The exact verified Alpha remains preserved as the non-latest `v0.1.0` upgrade baseline, and `v0.2.1` remains immutable. Public `v0.2.2` now contains both session-blocker corrections, passed the clean Windows release gate, and was freshly re-downloaded and verified; the tester also confirmed both corrections in the installed local candidate and reports being unable to break the close path. `VFM-130`–`VFM-150` remain complete.
- Active item: use the exact public `0.2.2` installer/checksum/evidence for the remainder of the defined 20–30 minute independent-user session and record environment, task completion, hesitation, coaching, recovery, PDF/printing, and willingness-to-use evidence. Do not mark `VFM-160` done until that complete session evidence is recorded; targeted blocker retests alone do not substitute for the remaining tasks.
- Following preparation: record environment, discovery, hesitation, coaching, recoverability, and printing availability without collecting private Verseform data. Fix any further writing-loss, scripture-correctness, or core-task blocker and retest that task before closing the item. Web implementation remains outside approved product scope.
- Known gap: the two `0.2.1` session blockers are corrected, clean-runner verified, publicly delivered in immutable `v0.2.2`, and targeted-native retested. The remainder of the independent session is still open, including exact public-installer handoff, environment/friction observations, save/reopen/recovery, offline/online translation behavior, multi-page PDF comparison, and printer availability. The immutable public `0.2.0` and `0.2.1` releases remain historical and unchanged.
- Triaged feedback: moving File/Edit/Help into the native title bar is intentionally outside the blocker patch because custom Windows chrome adds drag-region, window-control, DPI, and accessibility work. If “taskbar” meant the first in-app formatting row, that is a smaller follow-up; confirm the intended bar after `0.2.2` retesting.
- Release baseline: `.github/release-baselines/v0.1.0.json` pins Alpha source commit `0c41ff0ad845533679611ccb4c5bf969ceaf5e0a`, run `33759181934`, archive digest `b2d549e9596e8df6fe2ec1c22ac46dbd5a24ba44698cd1e87d4a096e3eb49ef8`, and the exact clean-runner-observed 4,197,262-byte unsigned installer SHA-256 `0caebf685ed7debfbb164b32a807871fb5d94026c471e1c03f23d2b025544001`. The patch job contains no expiring run-id dependency.
- Recovery baseline: Restore carries the selected recovery through `document.request`; the confirmation identifies it by name and captured time. Cancel retains current writing and its operations, Save restores only after an exact successful save, cancellation/failure/intervening edits retain the draft, and Discard alone switches documents. The switch clears persistence/output/preview/insertion owners, cancels timers and lookups, and serializes recovery writes/discards per document so late effects cannot overwrite identity or recovery state.
- Output baseline: Print and Save PDF freeze one attributed snapshot, then locally bundled Paged.js builds the same fixed Letter page tree for the dialog and offscreen WebView2 surface. Every page receives the complete escaped DBS footer and optional actual page number; measured footer overflow aborts output. Page-number changes repaginate only frozen title/body/notices, and stamped layout readiness prevents stale pages from reaching the native adapter. Cancel invokes no platform effect, Export alone advances to the native destination dialog, and the editor remains continuous.
- Writing baseline: the native title is the only visible Verseform/document identity; the glued File/Edit/Help and formatting deck stays visible while documents scroll, and no permanent starter hint competes with writing. Tab/Shift+Tab dispatch the same single-level indentation path as the toolbar. F6 moves from writing to the first detected reference and then File, Shift+F6 reverses through the last reference, arrows traverse references, and Escape returns to writing; valid and invalid decorations expose descriptive accessible names. The active translation uses the normalized citation abbreviation at rest and expands into a keyboard-accessible local search over full catalog titles. DBS chapter normalization removes only evidence-backed glued heading suffixes and repairs unambiguous punctuation spacing before preview or insertion.
- Trust boundary: React still has no adapter access. Paged.js receives only locally generated escaped semantic HTML and a bundled stylesheet; generated links are inert in review, no remote code or origin is loaded, and operation/mode/layout stamps guard every output boundary. Credits website intents travel through a stamped workspace effect to a typed external-link port; the Windows command maps only two fixed IDs to HTTPS constants and launches them in the default browser. Provider metadata remains React text, the webview CSP still has no remote origins, and opening Credits makes no scripture request.

## Last verified

- Clean Windows patch run `34072828527` passed at commit `7344480c5d32228ecc4050feaf02f1df9443338e`: durable Alpha verification, the canonical suite, release build, exact 0.1.0-to-0.2.2 data preservation, separate clean offline install/uninstall, Rust advisory audit, evidence upload, publication, and public re-download all succeeded. Artifact `verseform-0.2.2-windows-beta` is ID `10001327514`, retained through `2026-10-07T01:39:52Z`, with archive SHA-256 `780332fe7a36e3566f69ae535ec006b6bc958eddaab0e1d9c455237386b2150c`.
- Public pre-release `v0.2.2` targets that exact commit and exposes the exact three verified assets. A fresh direct download of the 4,310,375-byte installer returned SHA-256 `b8f41273a63e28f3d5d979d6faa164c7861f033cf39c41dac4eda0dba7595eb4` and explicit `NotSigned` status; the attached checksum and evidence identify the same bytes, run, commit, and durable Alpha provenance. GitHub records the checksum asset as SHA-256 `f0b6cdc5ef1c03c5c0f6fd40d269168164f541230d73ffb2465822f4613f8b61` and evidence asset as `1b47eb2b0dc43c6f722df1d405075ae715750f44ffe4d7e701fbb26e5eb3f539`.
- The tester installed the rebuilt local `0.2.2` candidate and reports being unable to break dirty X → Discard; it now closes the app as authorized. Together with the earlier shifted-reference retest, both session blockers have passed targeted native retesting. This does not yet replace the complete 20–30 minute independent-user session or the clean runner's installer/upgrade authority.
- The tester's native retest confirmed that expanding `john 1:1` before `jon 1:1` no longer strands the shifted Jonah reference. The same retest reached the dirty X-close confirmation, but Discard did not close the first local `0.2.2` build. Inspection proved that its Tauri capability allowed title changes but omitted the `destroy` command used after the kernel authorizes close; the adapter had also swallowed that rejection. The revised candidate grants that command only to the local main Windows window and reports any future close refusal while preserving dirty writing.
- `npm run check` passed for the revised `0.2.2` VFM-160 blocker patch on 2026-09-06: 45 pure/provider/kernel/controller/architecture tests plus one skipped live smoke, 40 production-browser journeys, 11 native unit tests plus one ignored live smoke, 7 Windows smoke tests, TypeScript, Rust formatting/Clippy, release boundaries, and 678 dependency-license records. The close proofs cover one platform guard, dirty Discard authorization, the exact reviewed force-destroy capability, and a rejected destroy remaining visible and recoverable. The shifted-reference regression remains green.
- The revised local release-mode desktop build produced unsigned `Verseform_0.2.2_x64-setup.exe` (4,310,327 bytes; SHA-256 `b86c05fd8100263574c8b518f4f4a68d3481b99939dfce54e673439cad072e5d`) on 2026-09-06. This confirms the corrected Windows shell compiles and bundles, but the tester's native close retest and the clean runner remain authoritative for runtime and distributable claims; the existing local Verseform profile was not touched.
- `npm run check` passed for the VFM-160 session blocker correction on 2026-09-05: 43 pure/provider/kernel/controller/architecture tests plus one skipped live smoke, 40 production-browser journeys, 11 native unit tests plus one ignored live smoke, 7 Windows smoke tests, TypeScript, Rust formatting/Clippy, release boundaries, and 678 dependency-license records. The new browser journey reproduces `john 1:1` followed by fuzzy `jon 1:1`, expands John first, verifies the surviving Jonah preview, then expands Jonah and checks both normalized citations. Public `0.2.1` still predates this source correction; patched delivery and independent-user retest remain required.
- Clean Windows patch run `33995590945` passed at commit `1da671920bffa5c373640278281d7c213aa95336`: durable Alpha verification, the canonical suite, release build, exact 0.1.0-to-0.2.1 upgrade preservation, separate clean offline install/uninstall, Rust advisory audit, evidence upload, publication, and public re-download all succeeded. Artifact `verseform-0.2.1-windows-beta` is ID `9978236778`, retained for 30 days, with archive SHA-256 `66d76c9effd7230765051400f26a921a82e7d0d365f16878c04b83fa539b1856`.
- Public pre-release `v0.2.1` targets that exact commit and exposes the exact three verified assets. A fresh direct download of the 4,309,127-byte installer returned SHA-256 `888a771cc705b214115b50e32298ddb6c5c3dd01b7d25d04ebf75be6fbf8a80a` and explicit `NotSigned` status; the attached checksum and evidence identify the same bytes, run, commit, and durable Alpha provenance. The archival `v0.1.0` pre-release targets `0c41ff0ad845533679611ccb4c5bf969ceaf5e0a` and exposes the 4,197,262-byte installer with SHA-256 `0caebf685ed7debfbb164b32a807871fb5d94026c471e1c03f23d2b025544001`.
- The repository's default GitHub Actions workflow permissions were restored to read-only immediately after publication. The release workflow itself keeps explicit least-privilege job scopes; the pre-created, exact Alpha tag avoids asking an Actions token to create a release tag at a historical workflow-bearing commit.
- `npm run check` passed for the `0.2.1` VFM-160 candidate on 2026-09-05: 43 pure/provider/kernel/controller/architecture tests (plus one intentionally skipped live smoke), 39 production-browser tests, 11 native unit tests (plus one intentionally ignored live smoke), 7 Windows smoke tests, TypeScript, Rust formatting/Clippy, release boundaries including the durable-baseline contract, and 678 dependency-license records.
- The local release-mode build produced `Verseform_0.2.1_x64-setup.exe` with product version `0.2.1` and explicit `NotSigned` status. The local Alpha-to-patch installer smoke correctly refused before mutation because an existing Verseform profile is present; that user data was preserved untouched. Installer upgrade/clean-lifecycle authority therefore remains with the isolated clean Windows workflow, not this workstation.
- `npm run check` passed for `VFM-150` on 2026-09-05: 43 pure/provider/kernel/controller/architecture tests (plus one intentionally skipped live smoke), 39 production-browser tests, 11 native unit tests (plus one intentionally ignored live smoke), 7 Windows smoke tests, TypeScript, Rust formatting/Clippy, release boundaries, and 678 dependency-license records.
- `VFM-150` focused proof passed on 2026-09-05: 27 output/kernel tests, all 5 dedicated output journeys, and 18 affected legacy browser journeys. One-page and representative multi-page previews agree with generated Chromium PDFs on Letter page count, content break markers, repeated DBS/WEB/long ENGTEST attribution, and optional actual page numbers; formatting, option repagination without recapture, cancellation, failure, focus containment, Axe A/AA, and offline no-provider-network behavior remain covered. `npm audit --audit-level=high` reports zero vulnerabilities, and the release-mode desktop build completed successfully.
- A real Windows WebView2 export was generated from the release executable and inspected as a four-page, 612×792-point Letter PDF. Pages contain reflection markers 1–16, 17–33, 34–50, and 51–58 respectively; `Powered by DBS`, the NASB notice, and Page 1–4 each appear exactly once per page, while the inserted citation appears once in the document. Rendering all four pages at 150 DPI showed consistent boundaries and no clipping or overlap. The first inspection exposed an application skip link in printed output; print isolation was corrected, locked with a browser regression, rebuilt, re-exported, text-checked, and visually re-inspected with no application chrome remaining.
- `npm run check` passed for `VFM-140` on 2026-09-05: 43 pure/provider/kernel/controller/architecture tests (plus one intentionally skipped live smoke), 38 production-browser tests, 11 native unit tests (plus one intentionally ignored live smoke), 7 Windows smoke tests, TypeScript, Rust formatting/Clippy, release boundaries, and 660 dependency-license records.
- `VFM-140` focused proof passed on 2026-09-05: 43 pure/provider/kernel/controller/architecture tests (plus one intentionally skipped live smoke), the production build, and 8 directly affected browser journeys. The new keyboard-only journey uses no `.focus()`, `.evaluate()`, or dispatched DOM event: it reaches valid and invalid references, previews, inserts, undoes, reaches File/Edit/Help and the translation picker, selects WEB, returns to writing, and preserves Tab/Shift+Tab indentation. Axe reports no A/AA violations with both reference states present.
- The release-mode Windows executable built successfully and was exercised through WebView2 with the computer-use accessibility tree and physical-key equivalents. The tree exposed the F6 instructions, a valid reference button, an invalid reference note with bounds, File/Edit/Help, and the translation control. F6 and arrows produced the corresponding cached valid/invalid previews, Shift+F6 returned to File with a visible focus ring, Enter inserted the attributed NASB passage, and Ctrl+Z restored the reference. WebView2 reports document-root focus rather than the active descendant through this automation API, so the changing tooltip, visible focus ring, content mutation, and accessible subtree are the native evidence; an independent screen-reader user remains part of `VFM-160`.
- `npm run check` passed for `VFM-130` on 2026-09-05: 42 pure/provider/kernel/controller/architecture tests (plus one intentionally skipped live smoke), including the Save/Discard/Cancel/failure/stale-result matrix and per-document recovery-effect ordering; 37 production-browser tests, including target identification, saved-draft preservation, canceled/failed Save, Discard, and a delayed lookup rejected after restore; 11 native unit tests (plus one intentionally ignored live smoke); 7 Windows smoke tests; TypeScript; Rust formatting/Clippy; release boundaries; and 660 dependency-license records.
- `npm run check` passed again on 2026-09-05 at reviewed commit `ddb3e23`: 39 pure/provider/kernel/controller/architecture tests, 33 production-browser tests, 11 native unit tests, 7 Windows smoke tests, TypeScript, Rust formatting/Clippy, release boundaries, and 660 dependency-license records. Live provider tests remained skipped. This review did not rerun installer/upgrade lifecycles, advisory network scans, or independent-user/native interactive acceptance.
- The 2026-09-05 browser review originally reproduced unguarded dirty-draft replacement by Restore, missing keyboard routes to references/commands, and an 80-line continuous PDF review with one Page 1/footer. `VFM-130`–`VFM-150` now close all three findings in source and add their acceptance paths to the canonical suite. Local ignored reproduction evidence remains in `artifacts/first-user-review-results.json`, `artifacts/first-user-review-preview.png`, and its `.mjs` script; the immutable public `0.2.0` binary still predates the corrections.
- `npm run check` passed on 2026-09-03: thirty-nine pure/provider/kernel/controller/architecture tests (plus one intentionally skipped live smoke), thirty-three production-browser tests, eleven native unit tests (plus one intentionally ignored live smoke), and seven Windows smoke tests, with clean TypeScript, Rust formatting, Clippy, CSP/capability checks, and 660 locked dependency-license records.
- The VFM-110 stability proof repeats six complete document/scripture/save/print/PDF cycles while reusing one cached chapter, crosses online → offline WEB → online preferred-translation sessions, and repeats provider/save/PDF failures without stranding commands or losing recovery. Cache tests cover exact TTL boundaries, stale offline reuse, successful refresh, and both 192-file/32-MB pruning policies; the recent profile remains bounded to ten entries. A direct DOM-event regression keeps reference clicks working after each New-document reset while preserving keyboard activation and stale-response rejection.
- The VFM-110 output proof covers an immutable modal preview; page-number changes without editor recapture; focus containment and return; Axe A/AA; cancellation before the platform effect and from the native-dialog adapter; an unwritable destination; provider-free offline output; and complete attributed multi-page PDF text. A native assertion locks Print to WebView2 browser-preview mode. The visual capture `artifacts/vfm-110-pdf-export-dialog.png` shows the reviewed desktop dialog.
- The earlier feedback proof remains green for one-level Tab/Shift+Tab indentation with retained focus; removal of the permanent hint; the complete two-row command deck pinned after a 500px document scroll; and recorded NASB James 1:1, 1:27, and 4:17 payload normalization while preserving a genuine glued sentence boundary. Representative-width, translation-picker, credits, document, recovery, scripture, keyboard, and walking journeys also remain green.
- The native allowlist unit proof rejects arbitrary URL and local-file input and accepts only the fixed DBS and eBible target IDs. Local visual captures show the writing-first chrome, the two-column Credits hierarchy, and the dedicated PDF review workflow at representative sizes.
- `npm audit --audit-level=high` reports zero vulnerabilities after adding the pinned pagination dependency. RustSec scanned all 474 locked Rust packages and reports no vulnerabilities; allowed maintenance warnings remain in Tauri's transitive graph, and the reported unsound GTK crate is absent from the Windows target graph.
- Clean Windows Beta run `33807242045` downloaded the exact retained Alpha, built the unsigned `0.2.0` installer, preserved seeded external document, profile, recovery, catalog cache, and chapter cache data through Alpha install, offline launch, in-place Beta upgrade, second offline launch, and uninstall, then passed a separate clean install/offline launch/uninstall lifecycle. Artifact `verseform-0.2.0-windows-beta` is retained through `2026-10-03T21:39:46Z`; installer SHA-256 is `efe2b30270afb22ee5a8e9d99ded947b8a44ac82aa13e8096527c706e2ecb241`.
- Public GitHub pre-release `v0.2.0` resolves directly to the verified candidate and publishes three uploaded assets. GitHub's asset digest for the 4,216,346-byte installer is `sha256:efe2b30270afb22ee5a8e9d99ded947b8a44ac82aa13e8096527c706e2ecb241`; a fresh direct download returned the same size and digest. The public Beta issue form renders its category, Windows/connectivity, safe-reproduction, expected-result, frequency/recovery, and required privacy fields from the default branch.
- GitHub Actions Windows alpha run `33759181934` passed on a clean Windows Server 2025 runner at alpha-closeout commit `0c41ff0`: npm and Rust advisory audits, the canonical regression suite, a fresh NSIS build, the installer lifecycle, and artifact upload all succeeded. Artifact `verseform-0.1.0-windows-alpha` is retained through 2026-09-17; the same run independently proved the refinement cases and the complete provider-free offline detect-to-insert/save/reopen/print/PDF flow.

## Handoff rule

Replace the **Now** and **Last verified** sections as work advances. Keep blockers only while they remain actionable. Detailed history belongs in version control, not this file.
